name: quran_sdk_example
description: Example app demonstrating quran_sdk usage
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  quran_sdk:
    path: ../

flutter:
  uses-material-design: true
